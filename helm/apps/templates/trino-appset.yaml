apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: trino
spec:
  generators:
    - git:
        repoURL: {{ .Values.source.repoURL }}
        revision: {{ .Values.source.targetRevision }}
        files:
          - path: clusters/*/trino-values.yaml   # points to the YAML files, not directories
  template:
    metadata:
      name: '{{`{{path.basename}}`}}'
      labels:
        app: '{{`{{path.basename}}`}}'
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: {{ .Values.project.name }}
      sources:
        # 1. Main Trino Helm chart
        - repoURL: {{ .Values.source.repoURL }}
          targetRevision: {{ .Values.source.targetRevision }}
          path: helm/trino
          helm:
            valueFiles:
              # - $values/helm/trino/values.yaml
              # - {{ printf "$values/clusters/%s/trino-values.yaml" .Values.source.clusterFolder | squote }}
              - $values/{{`{{path}}`}}/trino-values.yaml
        # 2. Reference repo to enable $values resolution
        - repoURL: {{ .Values.source.repoURL }}
          targetRevision: {{ .Values.source.targetRevision }}
          ref: values

      destination:
        server: {{ .Values.destination.server }}
        namespace: {{ .Values.trino.namespace }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
          - Validate=false