accessControl:
  enabled: false
  rules:
    rules.json:
      catalogs:
        - group: admin
          allow: all
      schemas:
        - group: admin
          allow: read-only

vaultSecret:
  enabled: true
  secretFile: |
    secret-key: dev-secret-value
    another-key: another-value

trino:
  server:
    node:
      environment: "dev_region_test"   # computed manually
    workers: 3
  jmx:
    enabled: true
